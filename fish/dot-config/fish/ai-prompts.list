# ai-prompts.list - AI 即插即用指令库
#
# 格式：分类|标题|Prompt 正文
# 规则：
#   - '#' 开头的行为注释，空行会被忽略
#   - 分类用于分组展示和关键词过滤
#   - 可随时编辑此文件增删改条目，无需修改代码（aip -e 可快速打开编辑）
#
# 设计原则（Agent 时代）：
#   - 不做角色扮演（Agent 自带身份）
#   - 不要求输出格式（Agent 直接改代码）
#   - 聚焦「维度清单 + 行为约束」
#
# 可复用的约束短语（可手动追加到任意 Prompt 末尾）：
#   [不引入新依赖]  [保持接口不变]  [不省略代码]
#   [先分析再动手]  [对齐项目现有风格]  [不改变业务逻辑]

# ── 起步与规划 ──────────────────────────────────────────────────────
起步|🚀 项目初始化（Project Setup）|基于目标技术栈和核心功能描述，设计一个生产级的项目骨架：目录结构、构建配置、关键依赖。遵循该技术栈社区的主流约定，不要过度设计。
起步|🎯 需求分解（Requirements）|帮我把目标想法拆解为 MVP 核心功能清单、业务流程图、技术难点评估和技术栈选型建议。先聚焦最小闭环，不要贪大。
起步|📐 产品设计（Product Design）|为目标产品定义：目标用户画像、核心价值主张、竞品差异点、MVP 功能边界、关键成功指标（North Star Metric）。约束：先聚焦一个细分用户群，不要试图满足所有人。
起步|🗄️  数据库设计（Schema Design）|基于目标核心业务逻辑，设计数据库 Schema：ER 关系图、表结构 DDL（含字段类型、索引、注释）。约束：考虑扩展性，避免过早优化。

# ── 功能实现 ──────────────────────────────────────────────────────
实现|💡 功能实现（Implement）|实现目标功能。约束：使用项目现有的技术栈和代码风格，优先复用已有模块，不要引入新依赖，不要过度设计。
实现|🎨 前端组件（UI Component）|基于目标描述或截图，实现前端 UI 组件。约束：使用语义化 HTML，提取可复用的 CSS 类，确保响应式适配，并处理各种临界状态（空数据、加载中、报错）。

# ── 代码审查 ──────────────────────────────────────────────────────
审查|📋 代码审查（Code Review）|审查目标代码。重点维度：潜在 Bug、安全漏洞、性能瓶颈、并发安全、代码异味。
审查|🔒 安全审计（Security Audit）|对目标代码做安全审计（参照 OWASP Top 10）。重点：注入、越权、信息泄露、不安全的反序列化。
审查|🌐 API 审查（API Review）|审查目标代码的 API 设计。重点：RESTful 规范、错误响应格式、入参校验、认证鉴权、版本管理、向后兼容性。
审查|📏 项目规范对齐（Convention Check）|检查目标代码是否与项目其他部分的风格、命名、错误处理、目录结构保持一致。如有偏离，请对齐到项目现有约定，不要自创新风格。

# ── 重构与优化 ──────────────────────────────────────────────────────
重构|🔄 代码重构（Refactor）|重构目标代码，提高可读性和可维护性。约束：严格保持原有功能和接口不变，不要引入新依赖，不要省略任何代码。
重构|✨ 代码清理（Cleanup）|清理目标代码：统一风格、改善命名、移除死代码和无用注释、消除代码异味。约束：不改变任何业务逻辑。
重构|⬆️  现代化改造（Modernize）|用当前语言版本的最新特性改造目标代码。替换已废弃的 API，采用更优雅的写法。约束：保持向后兼容，不改变公开 API 签名。
重构|👁️  极致可读性（Max Readability）|改写目标代码到极致可读：提取子函数、简化条件逻辑、添加战略性注释。目标：任何水平的开发者 30 秒内理解意图。约束：不改变业务逻辑。
重构|🗺️  重构路线图（Refactor Roadmap）|为目标代码输出一份分步骤的重构路线图（规划文档，不直接执行）。每一步需说明：改动范围、预期收益、风险评估。约束：每步必须可独立验证。

# ── 调试与排错 ──────────────────────────────────────────────────────
调试|🐛 错误调试（Debug）|定位目标代码中的问题。先分析根因，再给出最小化修复方案。约束：不要盲目试错，不要重构无关代码。
调试|🛡️  错误处理（Error Handling）|加强目标代码的错误处理：输入校验、边界检查、Guard Clauses、资源释放、错误传播策略。约束：不改变正常路径的业务逻辑。
调试|⚠️  并发安全（Concurrency）|检查目标代码的并发安全问题：竞态条件、死锁风险、资源泄漏、共享状态保护。约束：如需引入锁或同步机制，选择最小粒度的方案。
调试|🔍 自查验证（Self-Check）|回顾你刚才做的所有改动，逐一验证：功能是否完整保留、有无引入新 Bug、边界情况是否覆盖、是否符合项目约定。如果发现问题，直接修复。

# ── 工作流与闭环 ──────────────────────────────────────────────────────
流程|♻️ 自动修复闭环（Auto Remediation Loop）|按以下流程对代码进行自动修复与持续优化：1. 问题修复：逐一排查并修复项目中存在的缺陷、性能瓶颈、安全隐患及不规范之处。2. 原子化提交：每完成一类问题的修复或优化后，立即使用英文撰写符合 Conventional Commits 规范的 Git Commit 信息并提交，然后再继续处理下一个问题。3. 验收重评：当前阶段所有发现的问题均修复并提交后，对项目进行全局重新评审与打分。4. 质量循环：如果重新评审得分未达到 9.8 分，自动开启新一轮“排查 -> 修复 -> 提交 -> 重评”流程。注意：为避免无限死循环，最多允许自动循环执行 5 次。若 5 轮迭代后仍未达到 9.8 分，请停止执行，并输出当前最终得分及阻碍提分的遗留问题列表。

# ── 性能 ──────────────────────────────────────────────────────
性能|⚡ 性能优化（Optimize）|优化目标代码的性能。分析时间/空间复杂度，找出瓶颈（冗余循环、重复计算、I/O 阻塞），给出优化方案并标注复杂度变化。约束：不牺牲可读性。

# ── 测试 ──────────────────────────────────────────────────────
测试|🧪 单元测试（Unit Tests）|为目标代码编写单元测试。覆盖维度：正常路径、边界条件、异常情况。使用 Mock/Stub 隔离外部依赖，按 Arrange-Act-Assert 模式组织。
测试|🔁 TDD 驱动开发（Test First）|先为目标功能编写失败的测试用例。确认测试失败后，再编写最少量的代码使测试通过。约束：严格遵循红-绿-重构循环，每轮只解决一个测试。
测试|🔗 集成测试（Integration Tests）|为目标模块编写集成测试，验证各模块间的协作。用恰当的 Mock 策略处理外部服务和数据库依赖。
测试|📐 测试策略（Test Strategy）|为目标功能模块输出一份测试策略文档（规划文档，不直接执行）：单元/集成/手动测试的范围划分、关键测试场景、测试数据方案和验收标准。

# ── 文档 ──────────────────────────────────────────────────────
文档|📝 补充注释（Document）|为目标核心代码添加注释和文档字符串（JSDoc/Docstring/GoDoc 等）。包含：函数用途、参数说明、返回值、异常情况、使用示例。约束：不改变代码逻辑。
文档|📄 生成 README（Generate README）|为目标代码生成 README.md：项目简介、功能特性、安装步骤、使用示例、架构概览、配置说明。约束：基于实际代码内容，不要编造功能。
文档|✍️  营销文案（Copywriting）|为目标产品撰写营销文案：一句话价值主张、痛点共鸣、功能亮点描述、行动号召（CTA）。约束：禁止使用"赋能"、"一站式"、"革命性"等空洞词汇，用具体数据或场景说话。

# ── 解释与沟通 ──────────────────────────────────────────────────────
解释|📖 逻辑解释（Explain）|逐步解释目标代码的运行逻辑：目的、执行流程、关键设计决策。用通俗语言，避免术语堆砌，适合团队内部知识传递。
解释|🔭 代码库导航（Codebase Tour）|帮我理解目标项目的整体架构：入口在哪、核心数据流、关键模块的职责划分、模块间的依赖关系。用简洁的方式呈现全貌，不要逐行解释。
解释|📢 给非技术人员的总结（PM Summary）|用业务语言总结目标代码变更：改了什么、为什么改、对用户的影响、有无风险。适合发给产品经理或项目干系人。
解释|🌟 开源项目拆解（Project Teardown）|分析目标开源项目：它解决什么问题、核心架构与技术亮点、典型使用场景、值得借鉴的设计模式或技术。约束：基于项目实际代码和文档，不要泛泛而谈。

# ── 架构 ──────────────────────────────────────────────────────
架构|🏗️  架构审查（Architecture）|审查目标代码的架构设计。重点：可扩展性、关注点分离、依赖管理、模块耦合度。给出改进建议和目标架构方案。
架构|📦 依赖分析（Dependencies）|分析目标代码的依赖项：是否过时、已知安全漏洞、可替换的更轻量方案。给出升级或替换建议。
架构|🔀 代码迁移（Migration）|制定迁移计划，将目标代码从旧版本框架/语言升级到新版本。包含：Breaking Changes 清单、逐步迁移方案、回归测试要点。

# ── 日志与可观测性 ──────────────────────────────────────────────────────
运维|📊 日志优化（Logging）|优化目标代码的日志记录：结构化日志、合理的日志级别、关键上下文信息。约束：不泄露敏感数据，避免在热路径或循环内添加高频日志，确保日志对排障有实际价值。
运维|🚨 故障排查（Root Cause Analysis）|分析目标报错日志/堆栈信息，解释故障触发链条，定位根本原因（根因），给出修复方案。约束：先分析再动手，如果是 OOM、死锁等系统级问题，优先给出排查思路而非猜测性修复。
运维|🐧 脚本工具（Shell Script）|编写自动化脚本实现目标任务。约束：严格遵循 POSIX 标准，使用严格模式（set -euo pipefail），提供详尽的执行日志和错误提示。
