#!/bin/bash
# font-size
#
# Change font size of Ghostty.
#
# Requirements:
#   brew install gnu-sed

set -euo pipefail

RED='\033[0;31m'
GREEN='\033[0;32m'
YELLOW='\033[1;33m'
BLUE='\033[0;34m'
NC='\033[0m'

info() { echo -e "${BLUE}[INFO]${NC} $1"; }
success() { echo -e "${GREEN}[SUCCESS]${NC} $1"; }
warn() { echo -e "${YELLOW}[WARN]${NC} $1"; }
error() { echo -e "${RED}[ERROR]${NC} $1"; }

# Check for gsed (GNU sed)
if command -v gsed &>/dev/null; then
    SED_BIN="gsed"
elif command -v sed &>/dev/null; then
    SED_BIN="sed"
else
    error "sed not found"
    exit 1
fi

to_font_size=${1:-}

config_home=${XDG_CONFIG_HOME:-"$HOME/.config"}
ghostty_config="$config_home/ghostty/config"
dotfiles_repo_root=""

resolve_script_path() {
    local source_path="${BASH_SOURCE[0]}"
    local source_dir=""
    local target_path=""

    if [[ "$source_path" != */* ]]; then
        source_path="$(command -v "$source_path")"
    fi

    while [ -L "$source_path" ]; do
        source_dir="$(cd -P "$(dirname "$source_path")" && pwd)"
        target_path="$(readlink "$source_path")"
        if [[ "$target_path" = /* ]]; then
            source_path="$target_path"
        else
            source_path="$source_dir/$target_path"
        fi
    done

    source_dir="$(cd -P "$(dirname "$source_path")" && pwd)"
    printf '%s/%s\n' "$source_dir" "$(basename "$source_path")"
}

detect_dotfiles_repo_root() {
    local script_path=""
    local script_dir=""

    script_path="$(resolve_script_path)"
    script_dir="$(cd -P "$(dirname "$script_path")" && pwd)"

    if git -C "$script_dir/.." rev-parse --show-toplevel >/dev/null 2>&1; then
        git -C "$script_dir/.." rev-parse --show-toplevel
        return 0
    fi

    return 1
}

sync_ghostty_config_to_index() {
    if [ -n "$dotfiles_repo_root" ]; then
        git -C "$dotfiles_repo_root" add ghostty/dot-config/ghostty/config 2>/dev/null || true
    fi
}

reload_ghostty_if_running() {
    local reload_result=""

    if [ "$(uname -s)" != "Darwin" ]; then
        return 0
    fi

    if ! command -v osascript &>/dev/null; then
        return 0
    fi

    # 不能依赖 pgrep 判断 GUI App 是否在运行：
    # Ghostty 的实际进程名可能因宿主形态或大小写差异而变化，容易误判。
    # 这里改用 macOS 原生 AppleScript 查询应用进程，再直接触发 `reload_config`。
    if reload_result="$(osascript 2>/dev/null <<'EOF'
tell application "System Events"
    set ghostty_running to (exists application process "Ghostty") or (exists application process "ghostty")
end tell

if not ghostty_running then
    return "not-running"
end if

using terms from application "Ghostty"
    tell application "Ghostty"
        if (count of every terminal) is 0 then
            return "no-terminals"
        end if

        perform action "reload_config" on first terminal
        return "reloaded"
    end tell
end using terms from
EOF
)"; then
        case "$reload_result" in
            reloaded)
                success "Ghostty config reloaded automatically"
                ;;
            not-running)
                info "Ghostty is not running; the new font size will apply on next launch."
                ;;
            no-terminals)
                warn "Ghostty is running without open terminals; reload skipped"
                ;;
            *)
                warn "Ghostty auto-reload returned an unexpected result: $reload_result"
                ;;
        esac
    else
        warn "Ghostty auto-reload failed; press Cmd + Shift + , manually."
    fi
}

get_current_font_size() {
    if [ -f "$ghostty_config" ]; then
        grep -E '^font-size\s*=' "$ghostty_config" | "$SED_BIN" -E 's/^font-size\s*=\s*//' | head -1
    fi
}

if [ "$#" -ne 1 ]; then
    current="$(get_current_font_size)"
    printf 'Current font size: %s\n' "${current:-not set}"
    printf 'Usage: font-size <1-200>\n'
    exit 0
fi

if ! [[ "$to_font_size" =~ ^[0-9]+$ ]]; then
    error "font size must be an integer between 1 and 200"
    exit 1
fi

if [ "$to_font_size" -lt 1 ] || [ "$to_font_size" -gt 200 ]; then
    error "font size must be between 1 and 200"
    exit 1
fi

change_ghostty_font_size() {
    if [ ! -f "$ghostty_config" ]; then
        error "ghostty config not found: $ghostty_config"
        return 1
    fi

    if grep -qE '^font-size\s*=' "$ghostty_config"; then
        "$SED_BIN" -i -E "s/^font-size\s*=\s*.*/font-size = $to_font_size/" "$ghostty_config"
    else
        printf '\nfont-size = %s\n' "$to_font_size" >> "$ghostty_config"
    fi
}

old_font_size="$(get_current_font_size)"
change_ghostty_font_size
if dotfiles_repo_root="$(detect_dotfiles_repo_root)"; then
    sync_ghostty_config_to_index
fi
success "Ghostty font size: ${old_font_size:-not set} -> $to_font_size"
reload_ghostty_if_running
