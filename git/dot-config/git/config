[user]
    # user 信息已移出版本控制，不再公开。
    # 你的名字和邮箱现在由底部的 ~/.gitconfig.local 管理。

[include]
    # 引入本地个人身份与凭证，该文件不会被 dotfiles 追踪
    path = ~/.gitconfig.local

[includeIf "gitdir:~/work/"]
    # 如果你在 ~/work/ 及其子目录下工作，加载工作专属身份与配置
    path = ~/.gitconfig.work

[core]
    # 设置默认编辑器为 Helix
    editor = hx
    # 设置默认分页器为 Delta（语法高亮 Diff）
    pager = delta
    # 全局忽略文件路径（XDG 标准位置）
    excludesFile = ~/.config/git/ignore
    # 提交时转换为 LF，检出时不转换（适合 macOS/Linux）
    autocrlf = input

[interactive]
    # 交互式暂存时使用 Delta 渲染 Diff
    diffFilter = delta --color-only

[delta]
    # 使用 n/N 在 diff 块之间跳转
    navigate = true
    # 适配深色背景终端（如果是浅色终端改为 true）
    light = false
    # 显示行号
    line-numbers = true
    # 并排对比模式（左右分栏显示变更）
    side-by-side = false
    # 语法高亮主题（与 bat 共享 syntect 库，由 colorscheme 脚本管理）
    syntax-theme = Dracula

[delta "tokyonight-moon"]
    # Tokyo Night 官方 Delta 主题特性。
    # colorscheme 切到 `tokyonight` 时会自动在 [delta] 节中启用：
    #   features = tokyonight-moon
    true-color = always
    commit-decoration-style = blue ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = blue box
    hunk-header-file-style = red
    hunk-header-line-number-style = "#ff757f"
    hunk-header-style = file line-number syntax
    line-numbers = true
    line-numbers-left-format = "{nm:>4}┊"
    line-numbers-left-style = blue
    line-numbers-minus-style = "#e26a75"
    line-numbers-plus-style = "#65c6a8"
    line-numbers-right-format = "{np:>4}│"
    line-numbers-right-style = blue
    line-numbers-zero-style = "#3b4261"
    minus-emph-style = normal "#602d35"
    minus-style = normal "#37222c"
    minus-non-emph-style = normal "#37222c"
    plus-emph-style = normal "#1c4732"
    plus-style = normal "#20303b"
    plus-non-emph-style = normal "#20303b"
    syntax-theme = none

[delta "rose-pine"]
    # 自定义 Rose Pine Delta 主题。
    # colorscheme 切到 `rose-pine` 时会自动在 [delta] 节中启用：
    #   features = rose-pine
    true-color = always
    commit-decoration-style = "#31748f" ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = "#31748f" box
    hunk-header-file-style = "#eb6f92"
    hunk-header-line-number-style = "#f6c177"
    hunk-header-style = file line-number syntax
    line-numbers = true
    line-numbers-left-format = "{nm:>4}┊"
    line-numbers-left-style = "#908caa"
    line-numbers-minus-style = "#eb6f92"
    line-numbers-plus-style = "#9ccfd8"
    line-numbers-right-format = "{np:>4}│"
    line-numbers-right-style = "#908caa"
    line-numbers-zero-style = "#6e6a86"
    minus-emph-style = normal "#5f2d44"
    minus-style = normal "#2a1f33"
    minus-non-emph-style = normal "#2a1f33"
    plus-emph-style = normal "#213d48"
    plus-style = normal "#1f2b3b"
    plus-non-emph-style = normal "#1f2b3b"
    syntax-theme = none

[delta "kanagawa"]
    # 自定义 Kanagawa Delta 主题。
    # colorscheme 切到 `kanagawa` 时会自动在 [delta] 节中启用：
    #   features = kanagawa
    true-color = always
    commit-decoration-style = "#7E9CD8" ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = "#7E9CD8" box
    hunk-header-file-style = "#E46876"
    hunk-header-line-number-style = "#E6C384"
    hunk-header-style = file line-number syntax
    line-numbers = true
    line-numbers-left-format = "{nm:>4}┊"
    line-numbers-left-style = "#727169"
    line-numbers-minus-style = "#E46876"
    line-numbers-plus-style = "#98BB6C"
    line-numbers-right-format = "{np:>4}│"
    line-numbers-right-style = "#727169"
    line-numbers-zero-style = "#54546D"
    minus-emph-style = normal "#43242B"
    minus-style = normal "#2A1F24"
    minus-non-emph-style = normal "#2A1F24"
    plus-emph-style = normal "#2B3328"
    plus-style = normal "#1F2A22"
    plus-non-emph-style = normal "#1F2A22"
    syntax-theme = none

[delta "one-dark"]
    # 自定义 One Dark Delta 主题。
    # colorscheme 切到 `one-dark` 时会自动在 [delta] 节中启用：
    #   features = one-dark
    true-color = always
    commit-decoration-style = "#61AFEF" ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = "#61AFEF" box
    hunk-header-file-style = "#E06C75"
    hunk-header-line-number-style = "#E5C07B"
    hunk-header-style = file line-number syntax
    line-numbers = true
    line-numbers-left-format = "{nm:>4}┊"
    line-numbers-left-style = "#5C6370"
    line-numbers-minus-style = "#E06C75"
    line-numbers-plus-style = "#98C379"
    line-numbers-right-format = "{np:>4}│"
    line-numbers-right-style = "#5C6370"
    line-numbers-zero-style = "#4B5263"
    minus-emph-style = normal "#4A252C"
    minus-style = normal "#2F2227"
    minus-non-emph-style = normal "#2F2227"
    plus-emph-style = normal "#253A2C"
    plus-style = normal "#222D24"
    plus-non-emph-style = normal "#222D24"
    syntax-theme = none

[delta "everforest"]
    # 自定义 Everforest Delta 主题。
    # colorscheme 切到 `everforest` 时会自动在 [delta] 节中启用：
    #   features = everforest
    true-color = always
    commit-decoration-style = "#7FBBB3" ol
    commit-style = raw
    file-style = omit
    hunk-header-decoration-style = "#7FBBB3" box
    hunk-header-file-style = "#E67E80"
    hunk-header-line-number-style = "#DBBC7F"
    hunk-header-style = file line-number syntax
    line-numbers = true
    line-numbers-left-format = "{nm:>4}┊"
    line-numbers-left-style = "#859289"
    line-numbers-minus-style = "#E67E80"
    line-numbers-plus-style = "#A7C080"
    line-numbers-right-format = "{np:>4}│"
    line-numbers-right-style = "#859289"
    line-numbers-zero-style = "#7A8478"
    minus-emph-style = normal "#514045"
    minus-style = normal "#403238"
    minus-non-emph-style = normal "#403238"
    plus-emph-style = normal "#425047"
    plus-style = normal "#303B35"
    plus-non-emph-style = normal "#303B35"
    syntax-theme = none

[merge]
    # 冲突显示风格：同时显示本地、远程和基准代码（diff3），方便合并
    conflictstyle = diff3

[diff]
    # 智能检测代码移动（避免大段的删除+新增显示）
    colorMoved = default

[init]
    # 初始化新仓库时默认分支名为 main
    defaultBranch = main

[pull]
    # pull 时默认使用 rebase 而非 merge（保持提交历史线性整洁）
    rebase = true

[push]
    # 自动关联远程同名分支（无需每次 git push --set-upstream origin ...）
    autoSetupRemote = true

[rerere]
    # 自动记忆冲突解决方案（Reuse Recorded Resolution）
    # 相同的冲突再次出现时，Git 会自动应用之前的解决方式，极大提升 rebase 体验
    enabled = true

[credential]
    # 使用 macOS 钥匙串存储 HTTPS 密码
    helper = osxkeychain

[alias]
    # === 增强版日志 ===
    # lg: 显示漂亮的提交图谱（Hash, 时间, 提交信息, 作者, 分支）
    lg = log --graph --abbrev-commit --decorate \
        --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(auto)%d%C(reset)' \
        --all
    # lga: lg 的增强版，包含详细的提交时间
    lga = log --graph --abbrev-commit --decorate \
        --format=format:'%C(bold blue)%h%C(reset) - %C(bold cyan)%aD%C(reset) %C(bold green)(%ar)%C(reset)%C(auto)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim white)- %an%C(reset)' \
        --all
    # 查看最后一次提交
    last = log -1 HEAD

    # 清理已合并的本地分支（强迫症福音）
    cleanup = "!git branch --merged | grep -v '\\*' | xargs -n 1 git branch -d"

# === Git Clean Filter: 主题解耦配置 ===
# 该配置配合 .gitattributes 文件使用，用于解决 colorscheme 切换导致仓库变脏的问题。
# 当执行 git add 或 git status 时，Git 会调用 bin/dot-theme-filter 脚本处理匹配的文件。
# 脚本会将本地的主题名称、Ghostty 字号、Ghostty 背景透明度等显示偏好实时替换为预设默认值，
# 确保 Git index 中记录的内容始终保持干净。
# %f 参数用于将当前被处理的文件路径传递给过滤脚本，以便针对不同应用进行精准替换。
[filter "dot-theme"]
	clean = bin/dot-theme-filter %f
